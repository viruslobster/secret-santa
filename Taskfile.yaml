# https://taskfile.dev

version: "3"

tasks:
  default:
    deps:
      - build
      - serve
      - watch

  serve:
    cmds:
      - ./secret-santa

  frontend:
    cmds:
      - mkdir -p dist
      - elm make www/Main.elm --output dist/main.js

  backend:
    cmds:
      - go build

  watch:
    cmds:
      - elm-live www/Main.elm -- --output=dist/main.js --debug

  build:
    deps:
      - frontend
      - backend
